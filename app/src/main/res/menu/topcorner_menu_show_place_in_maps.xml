<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Locate me", should appear as action button if possible -->
    <item
        android:id="@+id/action_locate_show_maps"
        android:icon="@drawable/ic_my_location_white_24dp"
        android:title="Locate me"
        app:showAsAction="ifRoom"/>

    <!-- "Delete", should appear as action button if possible -->
    <item
        android:id="@+id/action_delete_show_maps"
        android:icon="@drawable/ic_delete_white_24dp"
        android:title="Delete"
        app:showAsAction="ifRoom"/>

    <!-- "Edit", should appear as action button if possible -->
    <item
        android:id="@+id/action_edit_show_maps"
        android:icon="@drawable/ic_edit_white_24dp"
        android:title="Edit"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <!--<item android:id="@+id/action_settings"-->
    <!--android:title="@string/action_settings"-->
    <!--app:showAsAction="never"/>-->

</menu>